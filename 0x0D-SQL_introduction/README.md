# Introduction to SQL

## Introduction

- SQL is a programming language used to query data from databases. It only does this on relational databases.
- Relational databases are databases whose data is arranged by tables, columns and rows.
- SQL uses data manipulation language to query info from databases. These include:
  - `SELECT`
  - `INSERT`
  - `UPDATE`
  - `DELETE`
  